services:
  - type: web
    name: agentic-research
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: BOCHA_API_KEY
        sync: false
      - key: PORT
        value: 10000
    autoDeploy: true
